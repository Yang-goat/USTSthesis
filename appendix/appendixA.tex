\chapter{附录A\quad 主要程序代码}

% 建议按模块拆分代码，保留必要注释；过长代码可放链接或仓库地址。
\begin{lstlisting}[language=Matlab,caption={示例：算法主函数框架}]
function [best_sol, best_fit] = solver(params)
    % 初始化
    N = params.pop_size;
    D = params.dim;
    pop = rand(N, D);
    v   = zeros(N, D);
    % 迭代
    for t = 1:params.max_gen
        fit = evaluate(pop);
        % TODO: 更新策略（示例占位）
    end
end
\end{lstlisting}

